Choix des structures : 
pour les ensemble (de noeud et d'arc) unordered_map car complixité de 1. 
Map complexité de O(log(n))

Vector <id, degré> , le tri est de compléxité O(n (log n)) avec n la taille de l'ensemble de nodes.

Concept de degre(n) : 
vérifier que 1<n<= this->V.size();
créer un vector <id node, degré>
le trier par degré
afficher les n premiers sous forme de tableau avec setw (set width)
|| id || degré || x || y ||
Vérification que n est dans la plage valide


débuger segmentation fault core dump -> trouver ou c'est 

